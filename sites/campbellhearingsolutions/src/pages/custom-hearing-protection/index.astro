---
import Base from "../../layouts/Base.astro";
import { getPageByRoute } from "../../lib/pages.js";

export const cacheHint = 3600;

const page = await getPageByRoute("/custom-hearing-protection/");

if (!page) {
  throw new Error("Missing dynamic page data for /custom-hearing-protection/");
}
---

<Base {...page}>
  <Fragment set:html={page.bodyHtml} />
</Base>
