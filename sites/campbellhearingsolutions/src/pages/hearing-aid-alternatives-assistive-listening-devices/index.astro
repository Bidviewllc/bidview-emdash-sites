---
import Base from "../../layouts/Base.astro";
import { getPageByRoute } from "../../lib/pages.js";

export const cacheHint = 3600;

const page = await getPageByRoute("/hearing-aid-alternatives-assistive-listening-devices/");

if (!page) {
  throw new Error("Missing dynamic page data for /hearing-aid-alternatives-assistive-listening-devices/");
}
---

<Base {...page}>
  <Fragment set:html={page.bodyHtml} />
</Base>
