---
import Base from "../../layouts/Base.astro";
import { getPageByRoute } from "../../lib/pages.js";

export const cacheHint = 3600;

const page = await getPageByRoute("/over-the-counter-hearing-aids/");

if (!page) {
  throw new Error("Missing dynamic page data for /over-the-counter-hearing-aids/");
}
---

<Base {...page}>
  <Fragment set:html={page.bodyHtml} />
</Base>
