---
import Base from "../../layouts/Base.astro";
import { getPageByRoute } from "../../lib/pages.js";

export const cacheHint = 3600;

const page = await getPageByRoute("/hearing-aids-products/");

if (!page) {
  throw new Error("Missing dynamic page data for /hearing-aids-products/");
}
---

<Base {...page}>
  <Fragment set:html={page.bodyHtml} />
</Base>
