---
import Base from "../../layouts/Base.astro";
import { getPageByRoute } from "../../lib/pages.js";

export const cacheHint = 3600;

const page = await getPageByRoute("/hearing-aids-for-tinnitus-relief/");

if (!page) {
  throw new Error("Missing dynamic page data for /hearing-aids-for-tinnitus-relief/");
}
---

<Base {...page}>
  <Fragment set:html={page.bodyHtml} />
</Base>
