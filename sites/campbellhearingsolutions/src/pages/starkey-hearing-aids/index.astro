---
import Base from "../../layouts/Base.astro";
import { getPageByRoute } from "../../lib/pages.js";

export const cacheHint = 3600;

const page = await getPageByRoute("/starkey-hearing-aids/");

if (!page) {
  throw new Error("Missing dynamic page data for /starkey-hearing-aids/");
}
---

<Base {...page}>
  <Fragment set:html={page.bodyHtml} />
</Base>
