---
import Base from "../../layouts/Base.astro";
import { getPageByRoute } from "../../lib/pages.js";

export const cacheHint = 3600;

const page = await getPageByRoute("/swimmers-ear-causes-symptoms-and-treatment/");

if (!page) {
  throw new Error("Missing dynamic page data for /swimmers-ear-causes-symptoms-and-treatment/");
}
---

<Base {...page}>
  <Fragment set:html={page.bodyHtml} />
</Base>
